const Title = ({title}) => {
  return (
    <>
        <h2 className="w-fit px-8 py-1 font-semibold border-2 border-[var(--primaryColor)] bg-[var(--secBg)] rounded-full">{title}</h2>
    </>
  )
}

export default Title